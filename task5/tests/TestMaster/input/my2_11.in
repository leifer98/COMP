int multipleReturns(int x) {
    if (x < 0) {
        return 1 - 2;
    } else if (x == 0) {
        return 0;
    }
    return 1;
}

void main() {
    printi(multipleReturns(5 - 10));
    printi(multipleReturns(0));
    printi(multipleReturns(5));
}
